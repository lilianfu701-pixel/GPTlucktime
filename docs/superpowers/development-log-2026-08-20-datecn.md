# DateCN 开发记录（2026-08-20）

## 1. 项目定位

- 对外域名：`datecn.org`
- 产品品牌：**DateCN**
- 产品定位：面向全球用户、不限定性别与地区的交友平台。
- 商业模式：基础能力可由后台开放或关闭，支持付费会员、套餐和权益配置。
- 独立工作区：`C:\Users\zexim\Documents\lucktime\.worktrees\global-dating-platform`
- 应用目录：`dating-platform`
- Git 分支：`codex/global-dating-platform`

## 2. 已完成并提交的系统基础

截至提交 `b67a94e`，已经完成并提交以下生产基础：

1. 账户注册、登录、会话、身份验证与安全策略。
2. 用户资料、照片上传、审核、偏好和隐私设置。
3. 搜索发现、候选快照、喜欢、收藏、匹配和访问记录。
4. 私信会话、消息历史、回执、实时连接和断线恢复。
5. 免费与付费会员权益、用量限制和后台配置。
6. Stripe 套餐、结账、订阅、退款、争议和对账流程。
7. 举报、内容隔离、风险限制、证据保全、申诉和人工审核。
8. 六角色运营后台、近期 MFA、双人审批、不可篡改审计和后台任务。
9. 敏感导出独立对象存储、自动清理和可靠消息投递。

最近的主要里程碑提交：

- `ab83521` — 订阅计费与对账。
- `8413d10` — 旧版媒体保全迁移与后台任务。
- `b67a94e` — 审计型运营后台。

## 3. 当前未提交工作

第 13 阶段正在工作区内，尚未提交：

- 中英文消息目录和 `next-intl` 接线。
- 通知偏好、安静时段、站内收件箱和通知后台任务。
- 隐私数据导出、加密对象、短期下载凭证和过期清理。
- 30 天账号删除冷静期、取消删除、订阅续费取消/恢复和法定保留。
- 唯一前向迁移 `0040_privacy_workflows.sql`。

该阶段已经历多轮规格审查。最后一个待复验点是：隐私导出下载凭证必须在成功下载后失效，同时读取失败时仍可恢复重试。实现已经完成，正在等待最终单点审查结论。

## 4. 最近验证状态

- Task 12 提交前全量测试：89 个测试文件通过、7 个环境门禁跳过；628 个测试通过、21 个跳过。
- Task 12：TypeScript、ESLint、Drizzle、生产构建和登录页启动检查全部通过。
- Task 13 最新定向验证：核心页面与隐私/通知测试通过；受影响的 Task 7/10/11/12 回归通过。
- 真实 PostgreSQL 并发门禁未执行，因为本地未配置 `TEST_DATABASE_URL`；相关测试明确跳过。
- 未写入真实密钥或生产数据。

## 5. 2026-08-20 产品方向调整

此前开发优先级过度偏向后台完整性、安全和异常边界，导致用户难以及时看到界面成果。现决定暂停继续扩大高级后台范围，转为“可见成果优先”。

下一阶段只聚焦：

1. 品牌首页。
2. 注册与登录。
3. 搜索发现与匹配。
4. 个人资料。
5. 消息聊天。
6. 会员中心。
7. 完整移动端界面和底部导航。

视觉方向已确认：**A · 暖金可信**；品牌统一使用 **DateCN**；第一版采用与真实数据隔离的演示数据，让页面无需注册即可完整展示。

## 6. 暂停事项

- Task 14、Task 15 暂不继续。
- 不继续扩展新的后台治理规则，除非它直接阻塞可见版页面。
- 不部署生产环境、不导入真实用户、不启用真实支付，直到可见版由用户确认。

